@page
@model Frontend.Pages.ListModel
@{
}
<h1>Listado Personas</h1>
<table class="table">
    @foreach (var persona in Model.ListaPersonas)
    {
        <tr>
            <td>@persona</td>
        </tr>
    }

</table>
